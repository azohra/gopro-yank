# Most people should run `gopro-yank login` instead. For a headless or automated
# setup, provide the gp_access_token cookie as an environment variable.

AUTH_TOKEN=eyJhbGciOi...replace-with-gp_access_token-value
