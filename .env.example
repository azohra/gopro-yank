# Prefer `gopro-yank login`. For non-interactive use, copy these GoPro cookies
# into the configured credential file or export them as environment variables.

AUTH_TOKEN=eyJhbGciOi...replace-with-gp_access_token-value
USER_ID=00000000-0000-0000-0000-000000000000
